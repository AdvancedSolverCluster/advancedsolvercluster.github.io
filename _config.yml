
# show_downloads: false
markdown: kramdown

kramdown:
  input: GFM
  syntax_highlighter: rouge
  math_engine: mathjax
  parse_block_html: true

collections:
  guide:
    permalink: "/:collection/:path"
    output: true
  guide_admin:
    permalink: "/guide/:path"
    output: true

defaults:
  - scope:
      path: "" # empty string for all files
      type: "guide"
    values:
      layout: "home"

just_the_docs:
  collections:
    guide:
      name: guide
    guide_admin:
      name: admin area

title:            'AdvancedSolver Cluster Guide Page'
tagline:          'Guide Page'
description:      'From just-the-docs template.'
url:              http://example.com
baseurl:          /
github:
  repo:           https://github.com/AdvancedSolverCluster/advancedsolvercluster.github.io/
logo: "/assets/images/logo/cash-logo.png"
favicon_ico: "/assets/images/logo/favicon/favicon-32x32.ico"

# Enable or disable the site search
# Supports true (default) or false
search_enabled: true

search:
  # Split pages into sections that can be searched individually
  # Supports 1 - 6, default: 2
  heading_level: 2
  # Maximum amount of previews per search result
  # Default: 3
  previews: 3
  # Maximum amount of words to display before a matched word in the preview
  # Default: 5
  preview_words_before: 5
  # Maximum amount of words to display after a matched word in the preview
  # Default: 10
  preview_words_after: 10
  # Set the search token separator
  # Default: /[\s\-/]+/
  # Example: enable support for hyphenated search words
  tokenizer_separator: /[\s/]+/
  # Display the relative url in search results
  # Supports true (default) or false
  rel_url: true
  # Enable or disable the search button that appears in the bottom right corner of every page
  # Supports true or false (default)
  button: false

theme: just-the-docs

url: https://advancedsolvercluster.github.io

aux_links:
  GitLab: https://gitlab.advancedsolver.com/
  Element: https://element.advancedsolver.com/
  Overleaf: https://overleaf.advancedsolver.com/

# Heading anchor links appear on hover over h1-h6 tags in page content
# allowing users to deep link to a particular heading on a page.
heading_anchors: true


# Footer content
# appears at the bottom of every page's main content
# Note: The footer_content option is deprecated and will be removed in a future major release. Please use `_includes/footer_custom.html` for more robust markup / liquid-based content.
footer_content: "Copyright &copy; 2020-2023 Advancedsolver Admin Team."

# Footer last edited timestamp
last_edit_timestamp: true # show or hide edit time - page must have `last_modified_date` defined in the frontmatter
last_edit_time_format: "%b %e %Y at %I:%M %p" # uses ruby's time format: https://ruby-doc.org/stdlib-2.7.0/libdoc/time/rdoc/Time.html

# Footer "Edit this page on GitHub" link text
gh_edit_link: true # show or hide edit this page link
gh_edit_link_text: "(Cluster Administrator Only)Edit this page on GitLab."
gh_edit_repository: "https://gitlab.advancedsolver.com/cluster-admin/server-management" # the github URL for your repo
gh_edit_branch: "main" # the branch that your docs is served from
# gh_edit_source: docs # the source that your files originate from
gh_edit_view_mode: "tree" # "tree" or "edit" if you want the user to jump into the editor immediately


callouts:
  warning:
    title: Warning
    color: red
  note:
    title: Note
    color: blue
  tip:
    title: Tip
    color: green
  important:
    title: Important
    color: yellow



plugins:
  - jekyll-redirect-from

whitelist:
  - jekyll-redirect-from